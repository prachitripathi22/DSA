class Solution {
public:
    void deleteNode(ListNode* node) {
        ListNode* temp=node->next;
        node->val=temp->val;
        node->next=temp->next;      
    }
};

//the element to be deleted is not the the last element or the tail element.
